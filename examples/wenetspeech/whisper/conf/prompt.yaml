<TRANSCRIBE>:
  - 转录如下音频。
  - 转录这段音频中的语音内容为文字。
  - 将音频的语音部分转换为文字形式，谢谢。
  - 将这段音频的语音内容详细记录为文字稿。
  - 执行语音识别任务，将音频转换为文字。
<TRANSCRIBE> <ALIGN>:
  - 请将音频内容转录出来，并为每个字都标注上时间戳，时间精度要达到0.01秒。
  - 将音频进行文字转换，并逐字标记时间，时间戳需精确到百分之一秒。
  - 请对音频进行逐字转录，并为每个字附上时间戳，时间单位最小为0.01秒。
  - 把音频转换成文字，要求每个字都有对应的时间戳，时间戳最小单位为0.01秒。
  - 将音频的每一句话都转录成文字，并精确到每个字的时间戳，时间精度需达到0.01秒。
<S2TCHAT>:
  - 实现语音与文字的即时交流，请进行回复。
  - 根据语音输入，直接以文字形式进行回答或对话。
  - 接收语音输入后，请转换为文字并给出相应回复。
  - 对语音问题直接进行对话交流。
  - 请将语音输入解析为文字信息，然后直接回复或对话。
<TRANSCRIBE> <BACKGROUND>:
  - 将语音转录并依据位置标注背景音，长背景静音用<lbkg>标记，短背景音用<sbkg>标记。
  - 录制语音并进行转录，同时按位置添加背景音标签，长静音为<lbkg>，短音为<sbkg>。
  - 语音转录时，需根据位置插入背景音标签，长段静音标为<lbkg>，短背景音标为<sbkg>。
  - 转换语音为文字，并依位置附上背景音标签，其中长背景静音为<lbkg>，短背景音为<sbkg>。
  - 对语音进行转录处理，同时标注背景音标签，长静音部分用<lbkg>表示，短背景音部分用<sbkg>表示。
<TRANSCRIBE> <EMOTION>:
  - 请将音频中的内容进行文字转换，并在结尾根据情感添加相应标签，标签种类有七种：<sad>，<anger>，<neutral>，<happy>，<surprise>，<fear>，<disgust>。
  - 将音频中的声音转换为文字描述，并在结尾附上一个情感标签，标签包含：<sad>，<anger>，<neutral>，<happy>，<surprise>，<fear>，<disgust>七种。
  - 请把音频中的内容逐字转录成文字，并在文末添加一个表达情感的标签，标签共有七种选择：<sad>，<anger>，<neutral>，<happy>，<surprise>，<fear>，<disgust>。
  - 将音频的声音转换为对应的文字内容，并在文本最后附上一个代表情感的标签，标签有：<sad>，<anger>，<neutral>，<happy>，<surprise>，<fear>，<disgust>七种可供选择。
  - 请对音频进行文字转录，并在转录完成的文本末尾添加一个情感标签，标签包含以下七种：<sad>，<anger>，<neutral>，<happy>，<surprise>，<fear>，<disgust>。
<TRANSCRIBE> <STYLE>:
  - 请将以下音频进行转录，并在文本末尾附加风格标签。标签分为五种：客服风格标记为<KEFU>，朗读或播音风格标记为<LANGDU>，古诗词风格为<POEM>，武侠评书风格为<PINGSHU>，以及童话故事风格为<STORY>。
  - 转录此音频内容，并在结尾处注明风格标签，标签有五类可选：客服风格<KEFU>，朗读或播音风格<LANGDU>，古诗词风格<POEM>，武侠评书风格<PINGSHU>，童话故事风格<STORY>。
  - 请将音频转录为文字，并在最后添加风格标签，标签分为客服<KEFU>、朗读/播音<LANGDU>、古诗词<POEM>、武侠评书<PINGSHU>、童话故事<STORY>五种。
  - 依照音频内容进行转录，并在文本末尾附上风格标签，标签包括客服风格<KEFU>、朗读或播音风格<LANGDU>、古诗词风格<POEM>、武侠评书风格<PINGSHU>和童话故事风格<STORY>。
  - 将音频转录成文，并在最后标注风格标签，标签类别有：客服<KEFU>风格、朗读/播音<LANGDU>风格、古诗词<POEM>风格、武侠评书<PINGSHU>风格和童话故事<STORY>风格。
<TRANSCRIBE> <CAPTION>:
  - 请把下列音频内容转录成文字，并在文档最后加上音频事件标签。标签类别包括：<cough>（咳嗽）、<cry>（哭泣）、<laugh>（笑声）、<screaming>（尖叫）、<sigh>（叹息）。
  - 请将以下音频转录成文字形式，并在结尾处添加音频事件标签。标签分为：<cough>、<cry>、<laugh>、<screaming>、<sigh>五种。
  - 请将这段音频转录，并在文本的最后附上音频事件标签，标签有：<cough>（咳嗽声）、<cry>（哭声）、<laugh>（笑声）、<screaming>（尖叫声）、<sigh>（叹气声）。
  - 请将下列音频转录为文本，并在文本末尾加上相应的音频事件标签，标签包括：<cough>、<cry>、<laugh>、<screaming>、<sigh>。
  - 请把这段音频内容转录出来，并在最后添加音频事件标签，标签分为五种，分别是：<cough>（咳嗽）、<cry>（哭泣声）、<laugh>（笑声）、<screaming>（尖叫）、<sigh>（叹息）。
<TRANSCRIBE> <GENDER>:
  - 请把以下音频内容转换成文字，并在文末加上性别标签，标签有两种：<female>或<male>。
  - 请将这段音频转录为文字，并在结尾处注明说话者的性别标签，标签为：<female>或<male>。
  - 请对以下音频进行文字转录，并在文本的最后附加性别标识，标识有两种选择：<female>，<male>。
  - 请将音频内容转录成文字形式，并在末尾添加性别标签，标签分为：<female>和<male>。
  - 请将下列音频转化为文字记录，并在记录末尾附上性别标签，标签包括：<female>，<male>。
<TRANSCRIBE> <AGE>:
  - 请把以下音频转换成文字，并在文本结束处加上年龄标签，标签有四种：<child>、<youth>、<middle_age>、<old>。
  - 请将这段音频进行文字转录，并在末尾附加说话者的年龄标签，标签分为：<child>、<youth>、<middle_age>、老年<old>。
  - 请对以下音频内容进行转录，并在转录文本的最后添加年龄标签，标签有：<child>、<youth>、<middle_age>、<old>四种。
  - 请将音频转录为文字，并在文字末尾注明说话者的年龄分类标签，标签包括：<child>、<youth>、<middle_age>、<old>。
  - 请将下列音频进行文字化处理，并在处理后的文本末尾附上年龄标签，标签分为：<child>、青少年<youth>、中年人<middle_age>、老年人<old>。